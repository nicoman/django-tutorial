==========
Django Lab
==========

Este proyecto ha sido realizado para el curso de introducción a Django.


Como instalar el proyecto?
==========================

- Crear un virtualenv
- Dentro del virtualenv instalar las dependencias del proyecto.
    pip install -r requirements.txt
- Crear local_settings.py y completar con datos propios
    cp local_settings_example.py local_settings.py
- Iniciar la base de datos para el proyecto.
    python manage.py syncdb --all
- Aplicar todas las migraciones
    python manage.py migrate --fake

#EOF
